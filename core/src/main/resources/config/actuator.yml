arsnova:
  management:
    endpoints:
      web:
        base-path: /management
        exposure:
          include: "*"
    endpoint:
      health:
        show-details: when-authorized
        roles:
          - ADMIN
          - MONITORING

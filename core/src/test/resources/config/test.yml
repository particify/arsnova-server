# Configuration overrides for test environment
arsnova:
  security:
    admin-accounts:
      - login-id: TestAdmin
        auth-provider: ARSNOVA
    authentication-providers:
      registered:
        enabled: false

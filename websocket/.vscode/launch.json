{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Server Websocket",
      "preLaunchTask": "Debug project",
      "projectRoot": "${workspaceFolder}",
      "request": "attach",
      "type": "kotlin",
      "hostName": "localhost",
      "port": 5005,
      "timeout": 5000
    }
  ]
}
